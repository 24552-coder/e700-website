# 【雙和醫院病歷組】公文透過 Google 郵件自動發送與進度追蹤系統 計畫書

## 一、 系統概述與建置目標
本系統係為雙和醫院病歷組量身打造之公文函詢與追蹤自動化平台。系統以現有之 Google 郵件 (Gmail / Google Workspace) 與 Google 雲端硬碟 (Google Drive) 為運作核心，免去微軟 O365 付費授權成本。

核心目標包含：
1. **雙層結構管理**：整合「公文主檔（20個欄位）」與「醫師函詢明細檔（24個欄位）」，公文主檔自動顯示函詢進度（如 `4/5 (80%)` 或 `不需醫師已完成`）。
2. **自動化 Google 信件雙向收發**：發送函詢信件時自動抄送 (Cc) `承辦 Email`、`副本 Email 1` 與 `副本 Email 2`；醫師直接點擊 Gmail 回覆信件，系統自動監聽解析並扭轉狀態為 `已回覆`。
3. **退回補件與結案機制**：提供手動退回補件（必填退回原因）與結案確認功能，並自動觸發對應通知信件。
4. **超大附件轉雲端連結解決方案**：偵測單檔 > 10MB 或總檔 > 25MB 時，自動轉存至 Google Drive 專屬目錄並於信件內文生成安全下載連結，防止郵件遭退件。
5. **每日催辦與逾期警示**：每日自動檢測未回覆案件（逾期 >= 1天），自動寄發催辦信並於儀表板提示紅字警示清單。
6. **未結案週表 Excel 匯出**：依據整合完成率 < 100% 條件，一鍵匯出符合標準格式之 Excel 週表。

---

## 二、 資料結構規範 (Data Schema)

### (一) 公文主檔 (Main Document) 欄位
1. `收發文號` (PK)
2. `病歷號`
3. `病患姓名`
4. `創稿文號`
5. `來文單位`
6. `發文日期`
7. `發文字號`
8. `承辦人員`
9. `病歷查詢費`
10. `勞保局受理編號`
11. `附件連結`
12. `函覆文號`
13. `函覆日期`
14. `主旨`
15. `備註`
16. `公文狀態` (處理中 / 不需醫師已完成 / 已完成)
17. `整合完成率` (如: 4/5 (80%))
18. `建立時間`
19. `最後更新時間`
20. `調病歷`

### (二) 醫師函詢明細檔 (Issue Master) 欄位
1. `函詢ID` (PK)
2. `收發文號` (FK)
3. `發文日期`
4. `病歷號`
5. `病患姓名`
6. `醫師姓名`
7. `醫師Email`
8. `副本Email1`
9. `副本Email2`
10. `承辦人員姓名`
11. `分機`
12. `承辦Email`
13. `問題摘要`
14. `醫師意見回覆`
15. `備註`
16. `函詢狀態` (待發送 / 已發送 / 已回覆 / 退回補件 / 已完成)
17. `回覆期限`
18. `寄件時間`
19. `回覆時間`
20. `最後提醒時間`
21. `提醒次數`
22. `建立時間`
23. `退回原因`
24. `附件檔案`

---

## 三、 五大標準 HTML 郵件範本

1. **填完醫師函詢問題自動寄信** (藍色 `#0D6EFD`)
2. **醫師回復後自動寄信** (鋼鐵藍 `#0B5ED7`)
3. **按退回補件按紐自動寄信** (警示紅 `#DC3545`)
4. **承辦同仁按已完成按紐自動寄信** (成功綠 `#198754`)
5. **超過一天醫師未回覆自動寄信** (逾期紅 `#C82333`)

---

## 四、 匯出週表規範
* **檔名**：`雙和醫院病歷組_公文函詢週表_YYYYMMDD.xlsx`
* **欄位**：`序號`｜`承辦人`｜`收發文號`｜`主旨`｜`寄件日期`｜`處理狀態`
* **篩選條件**：整合完成率 < 100% (即非已完成案件)
